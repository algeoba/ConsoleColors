#include <iostream>
#include <unistd.h>
#include <stdlib.h>

using namespace std;

int main()
{
	using namespace std;

	string a, b, c;
	float x, y, z;
	int i, j, k;

	int main() {
		i = 1;
		while (true) {
			sleep(1);
			system("clear");
			//a = to_string(rand()%9);
			//b = to_string(rand()%9);
			//c = a + b;
			system("Color 14");

			cout << i << endl << to_string(rand()) << endl;
			i++;
		}
	}
}
